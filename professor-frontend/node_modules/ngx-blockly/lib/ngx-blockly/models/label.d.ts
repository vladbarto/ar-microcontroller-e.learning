import { Node } from './node';
export declare class Label implements Node {
    kind: string;
    private _text;
    private _webClass;
    constructor(text: string, _webClass: string);
    get text(): string;
    set text(value: string);
    get webClass(): string;
    set webClass(value: string);
    toXML(): string;
}
