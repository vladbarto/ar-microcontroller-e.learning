import { Node } from './node';
export declare class Button implements Node {
    kind: string;
    private _text;
    private _callbackKey;
    constructor(text: string, callbackKey: string);
    get text(): string;
    set text(value: string);
    get callbackKey(): string;
    set callbackKey(value: string);
    toXML(): string;
}
